Start in background → docker compose up -d

Gracefully stop containers (but don’t remove them) → docker compose stop

Restart → docker compose restart

Stop & clean up → docker compose down --volumes
